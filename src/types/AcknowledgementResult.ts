/* c8 ignore next 4 — type-only file, no runtime code to cover */
/** A CodeRabbit acknowledgement comment found on a PR issue. */
export interface AcknowledgementResult {
  readonly commentId: number;
  readonly commentUrl: string;
}
